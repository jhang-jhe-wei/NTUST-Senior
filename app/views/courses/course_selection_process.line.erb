<%= raw((Kamiflex.hash(self) do
  carousel do
    bubble size: "mega" do
      header paddingAll: "20px", backgroundColor: "#0367D3", spacing: "md", paddingTop: "22px" do
        vertical_box do
          text "110學年度", color: "#ffffff66", size: "sm"
          text "校內選課流程", color: "#ffffff", size: "xl", flex: 4, weight: "bold"
        end
      end
      body do
        text "如有變更，依學校公告為準", color: "#b7b7b7", size: "xs"

        horizontal_box spacing: "lg", margin: "xl" do
          text "初選08/20", size: "sm", gravity: "center", wrap: true
          vertical_box flex: 0 do
            filler
            vertical_box cornerRadius: "30px", height: "12px", width: "12px", borderColor: "#EF454D", borderWidth: "2px" do
            end
            filler
          end
          text "登記課程志願序", gravity: "center", flex: 4, size: "sm"
        end

        horizontal_box spacing: "lg", height: "64px" do
          baseline_box do
            filler
          end
          vertical_box width: "12px" do
            horizontal_box flex: 1 do
              filler
              vertical_box width: "2px", backgroundColor: "#6486E3" do
              end
              filler
            end
          end
          text "系統抽選志願序(暫停選課)", gravity: "center", flex: 4, size: "xs", color: "#8c8c8c"
        end
        
        horizontal_box spacing: "lg" do
          text "截止08/30", gravity: "center", size: "sm", wrap: true
          vertical_box flex: 0 do
            filler
            vertical_box cornerRadius: "30px", width: "12px", height: "12px", borderColor: "#6486E3", borderWidth: "2px" do
            end
            filler
          end
          text "初選結束", gravity: "center", flex: 4, size: "sm"
        end

        horizontal_box spacing: "lg", height: "64px" do
          baseline_box flex: 1 do
            filler
          end
          vertical_box width: "12px" do
            horizontal_box flex: 1 do
              filler
              vertical_box width: "2px", backgroundColor: "#6486E3" do
              end
              filler
            end
          end
          text "即將開學", gravity: "center", flex: 4, size: "xs", color: "#8c8c8c"
        end

        horizontal_box spacing: "lg" do
          text "開學09/22", gravity: "center", size: "sm", wrap: true
          vertical_box flex: 0 do
            filler
            vertical_box cornerRadius: "30px", width: "12px", height: "12px", borderColor: "#6486E3", borderWidth: "2px" do
            end
            filler
          end
          text "全校加退選/加簽", gravity: "center", flex: 4, size: "sm"
        end

        horizontal_box spacing: "lg", height: "64px" do
          baseline_box flex: 1 do
            filler
          end
          vertical_box width: "12px" do
            horizontal_box flex: 1 do
              filler
              vertical_box width: "2px", backgroundColor: "#6486E3" do
              end
              filler
            end
          end
          text "先選先上，額滿為止，約兩週", gravity: "center", flex: 4, size: "xs", color: "#8c8c8c"
        end

        horizontal_box spacing: "lg" do
          text "上課10/05", gravity: "center", size: "sm", wrap: true
          vertical_box flex: 0 do
            filler
            vertical_box cornerRadius: "30px", width: "12px", height: "12px", borderColor: "#6486E3", borderWidth: "2px" do
            end
            filler
          end
          text "課程不得再變動", gravity: "center", flex: 4, size: "sm"
        end

        horizontal_box spacing: "lg", height: "64px" do
          baseline_box flex: 1 do
            filler
          end
          vertical_box width: "12px" do
            horizontal_box flex: 1 do
              filler
              vertical_box width: "2px", backgroundColor: "#6486E3" do
              end
              filler
            end
          end
          text "期中考過後", gravity: "center", flex: 4, size: "xs", color: "#8c8c8c"
        end

        horizontal_box spacing: "lg" do
          text "二退11/24", gravity: "center", size: "sm", wrap: true
          vertical_box flex: 0 do
            filler
            vertical_box cornerRadius: "30px", width: "12px", height: "12px", borderColor: "#6486E3", borderWidth: "2px" do
            end
            filler
          end
          text "二退會留紀錄，請三思", gravity: "center", flex: 4, size: "sm"
        end
      end
    end
    bubble size: "mega" do
      header paddingAll: "20px", backgroundColor: "#0367D3", spacing: "md", paddingTop: "22px" do
        vertical_box do
          text "110學年度", color: "#ffffff66", size: "sm"
          text "校內加簽流程", color: "#ffffff", size: "xl", flex: 4, weight: "bold"
        end
      end
      body do
        text "如有變更，依學校規定為準", color: "#b7b7b7", size: "xs"

        horizontal_box spacing: "lg", margin: "xl" do
          text "開學09/22", size: "sm", gravity: "center", wrap: true
          vertical_box flex: 0 do
            filler
            vertical_box cornerRadius: "30px", height: "12px", width: "12px", borderColor: "#EF454D", borderWidth: "2px" do
            end
            filler
          end
          text "全校加退選/加簽開始", gravity: "center", flex: 4, size: "sm"
        end

        horizontal_box spacing: "lg", height: "64px" do
          baseline_box do
            filler
          end
          vertical_box width: "12px" do
            horizontal_box flex: 1 do
              filler
              vertical_box width: "2px", backgroundColor: "#6486E3" do
              end
              filler
            end
          end
          text "寫信或是到上課教室向教授請求加簽",wrap: true, gravity: "center", flex: 4, size: "xs", color: "#8c8c8c"
        end

        horizontal_box spacing: "lg", cornerRadius: "30px" do
          horizontal_box flex: 1 do
            text "領取", gravity: "center", size: "sm"
          end
          vertical_box flex: 0 do
            filler
            vertical_box cornerRadius: "30px", width: "12px", height: "12px", borderWidth: "2px", borderColor: "#6486E3" do
            end
            filler
          end
          text "向教授取得授權碼", gravity: "center", flex: 4, size: "sm"
        end

        horizontal_box spacing: "lg", height: "64px" do
          baseline_box do
            filler
          end
          vertical_box width: "12px" do
            horizontal_box flex: 1 do
              filler
              vertical_box width: "2px", backgroundColor: "#6486E3" do
              end
              filler
            end
          end
          text "至選課系統授權碼專區，填入課程代碼及加簽碼",wrap: true, gravity: "center", flex: 4, size: "xs", color: "#8c8c8c"
        end

        horizontal_box spacing: "lg", cornerRadius: "30px" do
          horizontal_box flex: 1 do
            text "加簽", gravity: "center", size: "sm"
          end
          vertical_box flex: 0 do
            filler
            vertical_box cornerRadius: "30px", width: "12px", height: "12px", borderWidth: "2px", borderColor: "#6486E3" do
            end
            filler
          end
          text "授權碼加簽完成", gravity: "center", flex: 4, size: "sm"
        end

        horizontal_box spacing: "lg", height: "64px" do
          baseline_box do
            filler
          end
          vertical_box width: "12px" do
            horizontal_box flex: 1 do
              filler
              vertical_box width: "2px", backgroundColor: "#6486E3" do
              end
              filler
            end
          end
          text "課程已被加入課表",wrap: true, gravity: "center", flex: 4, size: "xs", color: "#8c8c8c"
        end

        horizontal_box spacing: "lg" do
          horizontal_box flex: 1 do
            text "截止10/05", gravity: "center", size: "sm", wrap: true
          end
          vertical_box flex: 0 do
            filler
            vertical_box cornerRadius: "30px", width: "12px", height: "12px", borderWidth: "2px", borderColor: "#6486E3" do
            end
            filler
          end
          text "選課結束", gravity: "center", flex: 4, size: "sm"
        end
      end
    end
  end
end).to_json)%>