<%image_urls = [
  "https://blog.wells.tw/NTUST-Senior/images/major1.png",
  "https://blog.wells.tw/NTUST-Senior/images/major2.png"
]%>
<%= raw([(Kamiflex.hash(self) do
  bubble do
    body do
      text "全校不分系選課指南",gravity: "center",size: "xl",align: "center"
      separator color: "#c2c2c2"
      text "最簡單的選課方式:",size: "sm", margin: "md",color: "#aaaaaa"
      text "決定一主修系，修習主修系大一的必修課", margin: "md",wrap: true
      separator color: "#c2c2c2"
    end
    footer do
      url_button "查詢各系必修課表","https://querycourse.ntust.edu.tw/querycourse/#/ClassSchedule?openExternalBrowser=1",style: "primary"    
    end
  end
end),
(Kamiflex.hash(self) do
  carousel do
    bubbles image_urls, size: :giga do |image_url|
      body paddingAll: "0px" do
        image image_url, size: :full, aspectRatio: "1764:1106", aspectMode: :fit, action: uri_action(image_url)
      end
    end
  end
end)
].to_json)%>
