  <%= form_with(model: feedback) do |form| %>
  <% if feedback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feedback.errors.count, "error") %> prohibited this feedback from being saved:</h2>
      <ul>
        <% feedback.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group text-white">
    <%= form.label :category, "報告種類" %>
    <%= form.select :category,[['學姊答非所問', '學姊答非所問'], ['希望新增功能', '希望新增功能'],['學姊壞掉了','學姊壞掉了']] ,{} , class: "form-control" %>
  </div>
  <div class="form-group text-white">
    <%= form.label :content, "報告內容" %>
    <%= form.text_area :content, rows: 7, placeholder: "請描述一下你遇到的狀況。", class: "form-control" %>
  </div>
  <div class="text-center">
    <%= form.submit "提交", class: "btn btn-light text-info" %>
  </div>
<% end %>
